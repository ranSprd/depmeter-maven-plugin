package net.kiar.maven.dependency.freshness.metric.reducer;

import net.kiar.maven.dependency.freshness.metric.ArtifactDependencyMetrics;

/**
 * Compute a entire metric values over all dependencies. Whereby compute can 
 * calculate the max, min or sum of all metrics.
 * 
 * @author ran
 */
public class OverallMetricReducer {
    
    
    public void consume(ArtifactDependencyMetrics metrics) {
    }
}
